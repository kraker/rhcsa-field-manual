# R runs from book/ when Quarto renders/previews. renv lives at the
# repo root so its files sit alongside pyproject.toml; this profile
# points renv at the parent directory as the project.
Sys.setenv(RENV_PROJECT = normalizePath(".."))
source("../renv/activate.R")
